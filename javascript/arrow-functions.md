# Arrow functions

```js
// classic function
function sum(a, b) {
  return a + b;
}

// arrow function
let sum3 = (a, b) => {
  return a + b;
};

// Simple functions on a single line
[1, 2, 3, 4].map((x) => Math.pow(x, 2));
// [ 1, 4, 9, 16 ]
```
